name: CI Matrix

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  CLICOLOR_FORCE: "1"
  FORCE_COLOR: "1"

jobs:
  lint:
    name: Formality Polyglot Lint & Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Rust Cache
        uses: swatinem/rust-cache@v2

      - name: Install Node (for Prettier & Markdownlint)
        uses: actions/setup-node@v7
        with:
          node-version: "lts/*"

      - name: Install Formality (fml)
        run: |
          FML_URL="https://github.com/arvinduh/formality/releases/latest/download/fml-x86_64-unknown-linux-gnu.tar.gz"
          curl -sSL "$FML_URL" | tar -xz
          sudo mv fml /usr/local/bin/

      - name: Verify toolchains & install missing surfaces
        run: fml doctor || fml install

      - name: Check native config synchronization (fml sync)
        run: fml sync --check

      - name: Check polyglot formatting (fml fmt)
        run: fml fmt --check

      - name: Run polyglot linters (fml lint)
        run: fml lint

  security-audit:
    name: Security Audit (cargo audit)
    runs-on: ubuntu-latest
    permissions:
      checks: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Run cargo audit
        uses: rustsec/audit-check@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  test:
    name: Test on ${{ matrix.os }}
    needs: lint
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: swatinem/rust-cache@v2

      - name: Setup Typst compiler
        uses: typst-community/setup-typst@v5

      - name: Run test suite
        run: cargo test --all-targets

      - name: Build documentation
        run: cargo doc --no-deps
